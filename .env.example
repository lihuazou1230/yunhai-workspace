# 高德地图「Web服务」Key（天气功能使用）
# 申请地址：https://console.amap.com/dev/key/app
# 注意：创建 Key 时「服务平台」必须选「Web服务」，选错会报 infocode 10009
# （规划文档里写作 VITE_WEATHER_KEY，两个名字都认；本文件用 VITE_AMAP_KEY）
VITE_AMAP_KEY=

# Supabase（第五阶段：用户系统 + 多设备任务同步）
# 1. https://supabase.com 新建项目 → Project Settings → API 复制 URL 与 anon public key
# 2. 用 anon key 就够：真正的权限控制在数据库 RLS 策略里（见 supabase/schema.sql）
# 3. 在 SQL Editor 执行 supabase/schema.sql，并在 Authentication → Providers 打开 Email（可选 GitHub）
# 留空时应用自动进入「本地模式」：功能全部可用，只是不做云同步、不强制登录
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Cloudflare Turnstile 人机验证（第五阶段：注册安全增强）
# 1. https://dash.cloudflare.com → Turnstile → Add site，拿到 siteKey（公开）与 secretKey（私密）
# 2. secretKey 填进 Supabase Dashboard → Authentication → Attacks Protection（启用 Captcha），不进代码
# 3. siteKey 填在这里（公开可进 Git；留空 = 不启用验证码，注册照常可用）
# 本地开发不必申请真钥匙，用官方测试 siteKey 即可：
#   1x00000000000000000000AA 总是通过（无感）｜2x00000000000000000000AB 总是拦截
VITE_TURNSTILE_SITE_KEY=
