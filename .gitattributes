# Windows 批处理脚本一律 CRLF。
# cmd.exe 解析「UTF-8 + 仅 LF」的 .bat 会把整段脚本切错行：
# 中文变乱码、报一堆「'st' 不是内部或外部命令」，脚本静默失效（run.bat 就踩过这个坑）。
*.bat text eol=crlf
*.cmd text eol=crlf

# 另外一条约定（git 属性管不了，靠编辑器兜）：
# 含中文的 .ps1 必须存成 **UTF-8 with BOM**。Windows PowerShell 4.0/5.1 会把无 BOM 的
# .ps1 按系统 ANSI(GBK) 读，中文变乱码、甚至字符串引号被吃掉导致解析失败
# （scripts\build-deploy-package.ps1 就踩过这个坑，已补 BOM）。
